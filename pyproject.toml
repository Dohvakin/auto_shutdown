[project]
name = "auto-shutdown"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.4",
    "pytest-playwright>=0.7.0",
    "pytz>=2025.1",
    "websockets>=14.2",
]
